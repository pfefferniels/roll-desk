import { Emulation } from 'linked-rolls';
type DynamicsProps = {
    forEmulation: Emulation;
    pathProps: React.SVGProps<SVGPathElement>;
};
export declare const Dynamics: ({ forEmulation: emulation, pathProps }: DynamicsProps) => JSX.Element;
export {};
